<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lee.supersuse.mapper.AdminRoleMapper">

    <select id="selectLoginInfo" resultType="java.util.Map">
        select * from t_ip order by time desc
    </select>
</mapper>